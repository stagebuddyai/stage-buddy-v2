# Stage Buddy V2 - Isolated Transformer Environment
# This environment has compatible dependency versions for SpeechBrain

# Core dependencies with version constraints to avoid conflicts
numpy>=1.24.0,<2.4
torch>=2.0.0
torchaudio>=2.0.0

# Speech processing
speechbrain>=0.5.16
librosa>=0.10.0

# HuggingFace (for model downloads)
transformers>=4.30.0
huggingface_hub>=0.16.0

# Audio file handling
soundfile>=0.12.0
